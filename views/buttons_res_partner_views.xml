<odoo>
    <data>
        <record model="ir.ui.view" id="oo_whatsapp_web_res_partner_form_button">
            <field name="name">oo.whatsapp.web.res.partner.form.button</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <!-- Smart button en el header junto a otros botones estadísticos -->
                <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                    <button
                        name="action_send_whatsapp"
                        type="object"
                        string="WhatsApp"
                        icon="fa-whatsapp"
                        class="oe_stat_button"
                        invisible="not mobile"
                    />
                </xpath>
                <!-- Botón junto al campo mobile como alternativa -->
                <xpath expr="//field[@name='mobile']" position="after">
                    <button
                        name="action_send_whatsapp"
                        type="object"
                        string="Enviar WhatsApp"
                        icon="fa-whatsapp"
                        class="btn-link"
                        invisible="not mobile"
                    />
                </xpath>
            </field>
        </record>
    </data>
</odoo>
